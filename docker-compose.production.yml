services:
  debt-command-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: debt-command-api
    restart: unless-stopped
    ports:
      - "3013:3001"
    env_file:
      - .env.production
    networks:
      - homelab-apps

networks:
  homelab-apps:
    external: true